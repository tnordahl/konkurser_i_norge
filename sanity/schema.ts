import { bankruptcy } from "./schemas/bankruptcy";
import { kommune } from "./schemas/kommune";
import { addressChange } from "./schemas/addressChange";
import { userPreferences } from "./schemas/userPreferences";

export const schemaTypes = [
  bankruptcy,
  kommune,
  addressChange,
  userPreferences,
];
